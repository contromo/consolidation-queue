[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "consolidation-queue"
version = "0.1.0"
description = "Local prototype for staged memory governance experiments."
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["cq*"]
