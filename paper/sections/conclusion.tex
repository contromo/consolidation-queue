\section{Conclusion}

Memory metrics can pass while memory policies fail. The CQ case study
demonstrates this concretely at two layers: extractor-gating blocks an
aggregate-passing false unlock before policy comparison, and identity-PFLC
shows why clustering, retrieval, or answer metrics cannot certify the
governable evidence handle needed for governance. The AMB LoCoMo replay adds a
published-output decomposition of third-party runs, while the LongMemEval v1
probe preserves a stable CQ loss that a single-variable repair and a symmetric
falsifier isolate as an answer-time readout-cardinality gap. The contribution
is methodological: the evaluation discipline makes those failures inspectable.
The discipline is portable to
other persistent-agent memory systems, and the CQ artifacts and preregistration
locks are released as worked examples of how to apply it.
